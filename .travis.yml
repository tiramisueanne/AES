language: cpp
compiler: gcc

before_install:
  - sudo apt-get install libgtest-dev
  - sudo apt-get install cmake
  - cd /usr/src/gtest
  - sudo cmake CMakeLists.txt
  - sudo make
  - sudo cp *.a /usr/lib
  - cd ../../..

script:
  - cd AES
  - make
  - make test
